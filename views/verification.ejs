<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verification</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f7fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            text-align: center;
            background-color: white;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 90%;
            max-width: 500px;
        }

        h1 {
            color: #333;
            font-size: 2em;
        }

        p {
            color: #555;
            font-size: 1.1em;
        }

        .footer {
            margin-top: 20px;
            font-size: 0.9em;
            color: #777;
        }

        
    </style>
</head>
<body>

    <div class="container">
        <h1>Verifying Your Request...</h1>
        <p>Please wait while we verify your connection.</p>
        <div class="loading">This may take a few seconds...</div>
    </div>

    <script>
        setTimeout(() => {
            const params = new URLSearchParams(window.location.search);
            const userId = params.get('userId');
            window.location.href = `/redirect/${userId}`;
        }, 5000); // Delay 5 seconds
    </script>
    
</body>
</html>
